In this puppeteer is implemented to generate PDF file from HTML template

1. clone this repository
1. install all dependencies  
    ```npm i```  

3. change html template from ```data.js```, if you want
1. then simply run command  
```node index.js```  

And your PDF will be generated in the root of the directory